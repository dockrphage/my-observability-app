# My Observability App - Deployment Guide

## Prerequisites
- Docker Engine 20.10+
- Docker Compose v2+ (or use `docker compose` command)

## Quick Deploy

1. Extract these files to a directory
2. Run:
```bash
chmod +x *.sh
./start.sh
